Drop your photos here:

  photo1.jpg  →  used in the Hero section (the big card on the right)
  photo2.jpg  →  used in the About section

Notes:
- File names must match exactly (lowercase, .jpg extension).
- If you prefer .png/.webp, rename your files OR update the src paths in:
    src/components/Hero.jsx     →  /photos/photo1.jpg
    src/components/About.jsx    →  /photos/photo2.jpg
- Images are cropped via CSS (object-fit: cover). Portrait photos work best.
- If a photo is missing, the site shows initials/fallback automatically.
